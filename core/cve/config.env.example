# CVE多源同步管理器配置
# 将此文件复制到 .env 或作为环境变量

# GitHub Personal Access Token (可选, 提高API速率限制)
# 未认证: 60 req/hour -> 已认证: 5000 req/hour
# 创建Token: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# NVD API Key (可选, 提高API速率限制)
# 未认证: 5 req/30s -> 已认证: 50 req/30s
# 申请API Key: https://nvd.nist.gov/developers/request-an-api-key
NVD_API_KEY=your_nvd_api_key_here

# 数据库路径 (可选, 默认: data/cve_index.db)
# CVE_DB_PATH=/custom/path/to/cve.db

# 缓存目录 (可选, 默认: 系统临时目录/cve_cache)
# CVE_CACHE_DIR=/custom/cache/dir

# 订阅过滤配置 (示例)
# CVE_SUBSCRIPTIONS=apache,nginx,kubernetes
# CVE_MIN_CVSS=7.0
# CVE_SEVERITY_FILTER=CRITICAL,HIGH
